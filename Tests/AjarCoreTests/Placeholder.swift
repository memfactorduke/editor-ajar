// SPDX-License-Identifier: GPL-3.0-or-later
//
// AjarCoreTests — fast, headless unit + property tests for AjarCore (docs/TESTING.md §1).
//
// STATUS: scaffold only. The build loop adds tests here alongside each AjarCore feature, starting
// at ROADMAP M1 (time math, model, keyframes, the edit reducer + undo, .ajar round-trip).
// Intentional placeholder so the test target exists.
