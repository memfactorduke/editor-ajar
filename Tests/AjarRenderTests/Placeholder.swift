// SPDX-License-Identifier: GPL-3.0-or-later
//
// AjarRenderTests — golden-frame tests for the GPU compositor (docs/TESTING.md §2, ADR-0011).
// These render frames via the `ajar` harness and compare against stored references within a
// perceptual tolerance.
//
// STATUS: scaffold only. The build loop adds golden-frame tests here starting at ROADMAP M2.
// Intentional placeholder so the test target exists.
