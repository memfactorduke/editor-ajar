# Placeholder reference (FR-FX-002)

This effect is GPU-only (FR-FX-007). The committed `reference.png` is a solid-black
placeholder so the fixture layout is reviewable. The orchestrator establishes the
real reference from the CI GPU runner (ADR-0017 §6). Do not loosen tolerances to
force a pass against this placeholder.
