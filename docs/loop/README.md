# Autonomous build loop — working directory

Reserved for the autonomous build loop (ADR-0014): its task queue / backlog, run logs, and any
loop-specific state derived from [SPEC](../SPEC.md) and [ROADMAP](../ROADMAP.md).

Deliberately empty for now — wiring the loop is a **later** step (the orchestrator chose to set up
the repository first). The repo is already loop-ready: headless-testable core (ADR-0005),
deterministic rendering (ADR-0009), and machine-checkable quality gates (ADR-0011).
